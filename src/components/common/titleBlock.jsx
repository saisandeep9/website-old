import React from "react";

const Title = () => {
  const style = {
    width: "60px",
    height: "2px",
    margin: "5px auto 8px auto",
    background: "#ffff",
  };

  return <div style={style}></div>;
};

export default Title;
